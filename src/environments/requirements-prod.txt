# SIADS 699 Capstone - Production Requirements
# CPU-only deployment for Docker/Streamlit

# Include base requirements
-r requirements.txt

# PyTorch CPU-only (smaller, faster for inference)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1+cpu
torchvision==0.18.1+cpu
torchaudio==2.3.1+cpu
